= Document with Index
:name: Ada
:styled: *attribute bold*
:linked: https://example.com[Ada]

== Introduction

This document covers ((terminal rendering)) and ((AsciiDoc processing)).

We also discuss (((formatting, inline, bold))) and (((formatting, inline, italic))).

Advanced topics include (((rendering, output, terminal))) and (((rendering, output, HTML))).

== Content

The ((parser)) handles all input processing.

Direct formatting: ((bold *primary*)), ((italic _primary_)), and
indexterm2:[mono `primary`].

Attributes: ((plain {name})), ((literal {styled})), and ((linked {linked})).

Direct link: ((direct https://example.com[Ada])).

Formatted hierarchy: indexterm:[*Animals*, _Mammals_, `Cats`].

Formatting is identity: ((identity)), ((*identity*)), and ((_identity_)).

[subs="macros,attributes,quotes"]
Late substitutions: ((late {name} and {styled})).

[index]
== Index
