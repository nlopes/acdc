= Index Terms Test

This is about ((bold *Arthur*)) the king.

(((italic _Sword_, secondary `Broadsword`, tertiary)))A concealed term for swords.

Using macro style: indexterm2:[mono `Excalibur`].

And concealed macro: indexterm:[bold *Knights*, Round Table, tertiary].

Quoted hierarchy: indexterm:["primary, one", "secondary, two", tertiary].

Nested terms stay literal: ((outer indexterm:[inner])).
